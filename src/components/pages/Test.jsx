import React from 'react';
import PropTypes from 'prop-types';

const Test = props => {
    return (
        <div>
            Test Page
        </div>
    );
};

Test.propTypes = {
    
};

export default Test;